# Enable input completion
$schema: https://raw.githubusercontent.com/kota65535/firepit/refs/heads/main/schema.json

tasks:
  # Task name
  hello:
    # The command content is executed in shell
    command: npx -y cowsay "hello,firepit"
