env:
  B: b-bar
  C: c

env_files:
  - .env.bar
  - .env.root

shell:
  command: shell-bar
  args:
    - args-bar

tasks:
  bar:
    command: echo "bar"
  bar-2:
    command: echo "bar-2"
    shell:
      command: shell-bar-2
      args:
        - args-bar-2
    env:
      B: b-bar-2
      E: e
