projects:
  foo: foo
  bar: bar

env_files:
  - .env.root

shell:
  command: shell-root
  args:
    - args-root

env:
  A: a
  B: b

depends_on:
  - "#install"

tasks:
  install:
    command: echo "install"
    depends_on:
      - "install"
