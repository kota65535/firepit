projects:
  foo: foo
  bar: bar

vars:
  X: x
  XX: "{{ X }}-{{ X }}"

env:
  A: a-{{ X }}
  B: b-{{ XX }}

shell:
  command: shell-root
  args:
    - args-root

depends_on:
  - "#install"

tasks:
  root:
    command: echo "root {{ X }}"
  install:
    command: echo "install"
    env:
      C: c
    depends_on:
      - "install"
