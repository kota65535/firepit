includes:
  - "{{ root_dir }}/b.yml"

vars:
  bar: bar
  B: bar

env:
  bar: bar
  B: bar

env_files:
  - .env.bar

tasks:
  bar:
    command: echo "bar"
  bar-2:
    command: echo "bar-2"
    shell:
      command: shell-bar-2
      args:
        - args-bar-2
