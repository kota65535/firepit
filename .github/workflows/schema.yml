name: Schema

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: ${{ github.head_ref }}

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo dependencies
        uses: Swatinem/rust-cache@98c8021b550208e191a6a3145459bfc9fb29c4c0 # v2

      - name: Setup other tools
        uses: jdx/mise-action@d16887ba50704baed7de72bd1e82e04391e4457a # v3

      - name: Generate schema.json
        run: cargo run --bin firepit-schema

      - name: Generate schema-body.md
        run: ./scripts/schema-to-md.js schema.json docs/schema-body.md

      - name: Configure git
        uses: kota65535/github-git-config-action@c237550e392b6d305c1102ea2dc0f14eb755c4ac # v2
        with:
          user.name: github-actions[bot]
          user.email: 41898282+github-actions[bot]@users.noreply.github.com
          github-token: ${{ secrets.PERSONAL_TOKEN }}

      - name: Diff
        id: diff
        run: |
          git add -N .
          git diff --name-only --exit-code
        continue-on-error: true
      - name: Commit & Push
        if: steps.diff.outcome == 'failure'
        run: |
          git add .
          git commit --author=. -m 'generate schema'
          git pull --rebase --autostash
          git push origin HEAD
